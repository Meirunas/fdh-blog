<script is:inline>
    // Define colors for categories
    const categoryColors = {
        personal: "blue",
        technical: "green",
    };

    // Function to determine category and set color
    function setCategoryLinkColors() {
        document.querySelectorAll("a.tag").forEach((link) => {
            const href = link.getAttribute("href"); // Get the href attribute
            if (href.includes("/tag/personal")) {
                link.style.textDecorationColor = categoryColors.personal; // Apply 'personal' color
            } else if (href.includes("/tag/technical")) {
                link.style.textDecorationColor = categoryColors.technical; // Apply 'technical' color
            } else {
                link.style.textDecorationColor = "black"; // Default color for unknown categories
            }
        });
    }

    // Optional: Hover effect for tags
    function setHoverListeners() {
        document.querySelectorAll("a.tag").forEach((link) => {
            link.addEventListener("mouseover", () => {
                link.style.color = "darkgray"; // Example hover effect
            });
            link.addEventListener("mouseout", () => {
                link.style.color = ""; // Revert hover effect
            });
        });
    }

    // Immediately set colors on page load
    (function () {
        setCategoryLinkColors();
        setHoverListeners();
    })();
</script>
